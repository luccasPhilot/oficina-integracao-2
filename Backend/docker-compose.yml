services:
  postgres:
    image: postgres:latest
    container_name: postgres-db-ellp
    networks:
      - my_network
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ellp_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

networks:
  my_network:
    driver: bridge

volumes:
  postgres_data:
    name: ellp_volume
#docker-compose -f "docker-compose.yml" up -d
